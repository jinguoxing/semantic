好，这一步我直接给你一份**「语义版本 · Figma 结构稿（Frame 级）」**，目标是：

* **设计一看就能画**
* **前端一看就知道哪些是复用、哪些是新增**
* **和你现在的「业务对象建模页」强复用，不推翻**

我会按 **页面层级 → Frame → 区块 → 关键组件** 来给你。

---

# 语义版本 · Figma 结构说明（v2.4）

> 设计目标：
> **同一套页面，在「工作态 / 版本态」下切换**
> 语义版本态 = 只读快照 + Diff + 治理上下文

---

## 一、Figma 页面总览（Page List）

```text
📄 Semantic Governance
 ├── Semantic Version List
 ├── Semantic Version Detail
 │    ├── Overview
 │    ├── Object Snapshot   ⭐ 复用对象建模页
 │    ├── Quality & Security Snapshot
 │    ├── Downstream Binding
 │    └── Version Diff
```

---

## 二、Page 1：Semantic Version List（语义版本列表）

### Frame：`SemanticVersion/List`

#### 1️⃣ 顶部 Header

* Title：`语义版本`
* Subtitle：`冻结后的语义 / 质量 / 安全治理事实`
* CTA（右侧）：

  * `新建版本（草稿）`
  * `帮助 / 说明`

---

#### 2️⃣ Filter Bar（复用表格筛选组件）

* 版本状态：Draft / Published / Deprecated / Rolled Back
* 范围：逻辑视图 / 域 / 项目
* 创建人
* 发布时间

---

#### 3️⃣ Version Table

**列定义（固定）**

* Version
* Status
* Scope
* Objects
* Fields
* Quality Rules
* Security Policies
* Published At
* Actions

**Row Actions**

* 查看
* Diff
* 发布 / 回滚

---

## 三、Page 2：Semantic Version Detail（语义版本详情）

### Frame：`SemanticVersion/Detail`

```text
┌────────────────────────────────────┐
│ Version Header                      │
├────────────────────────────────────┤
│ Tab Navigation                     │
├────────────────────────────────────┤
│ Tab Content                        │
└────────────────────────────────────┘
```

---

### 2.1 Version Header（所有 Tab 共用）

**Frame：`SemanticVersion/Detail/Header`**

* Version：`v1.1.0`
* Status：`已发布`
* Scope：`HR 域 / 9 个对象`
* Base Version：`v1.0.2`
* Published At / By
* CTA（右侧）：

  * `查看 Diff`
  * `回滚`
  * `导出`

> 🔒 版本态：所有 CTA 都是**只读/治理级动作**

---

### 2.2 Tab Navigation

**Frame：`SemanticVersion/Detail/Tabs`**

Tabs 顺序（强烈建议）：

1. 对象快照
2. 字段语义
3. 质量 & 安全
4. 下游绑定
5. Diff

---

## 四、Tab 1：Object Snapshot（对象快照）

> ⭐ **核心：直接复用你现在的「业务对象建模页」**

### Frame：`SemanticVersion/Detail/ObjectSnapshot`

#### 左侧：对象列表（复用）

* 业务域树
* 对象卡片
* 状态 Badge：`Included / Changed / New`

#### 中部：对象属性区（复用）

* 对象名称（只读）
* 属性列表（只读）
* 属性来源标识：

  * 来源表
  * 字段语义版本

#### 右侧：物理表映射（复用）

* 表名
* 字段
* 映射线（只读）
* 映射说明 Tooltip：

  * 来源字段语义 vX
  * 是否与上版本一致

🚫 禁用能力：

* 新增对象
* AI 自动映射
* 拖拽 / 编辑
* 保存

---

## 五、Tab 2：Field Semantic Snapshot（字段语义快照）

### Frame：`SemanticVersion/Detail/FieldSemantic`

#### 1️⃣ Summary Bar

* 字段总数
* 已确认
* 冲突字段
* 覆盖率 %

#### 2️⃣ Field Table（只读）

列：

* 表名
* 字段名
* Term
* Role
* Tags
* Domain
* Decision By
* Changed（vs base）

支持：

* Filter（Changed / Unchanged）
* 跳转到字段详情（版本态）

---

## 六、Tab 3：Quality & Security Snapshot

### Frame：`SemanticVersion/Detail/QualitySecurity`

#### Section A：质量快照

* Rule Set Version
* Rule Count
* Last Run Summary（Pass Rate / Failed Fields）
* 查看质量详情（只读）

#### Section B：安全快照

* 分类分级分布
* 脱敏规则数
* 高风险字段数
* 查看安全策略（只读）

---

## 七、Tab 4：Downstream Binding（下游绑定）

### Frame：`SemanticVersion/Detail/Binding`

* Consumer List：

  * 问数
  * API
  * 服务
* 当前绑定版本
* 切换记录（只读审计）

---

## 八、Tab 5：Version Diff（版本差异）

### Frame：`SemanticVersion/Detail/Diff`

#### Diff Tabs：

* 对象 Diff
* 字段语义 Diff
* 质量规则 Diff
* 安全策略 Diff

#### Diff 展示方式：

* Added / Removed / Changed
* 可折叠
* 高亮变更字段

---

## 九、全局组件（必须统一）

### 1️⃣ Version Context Switcher

**Component：`VersionContextSwitch`**

* 工作态
* 查看版本 vX（只读）

用于：

* 逻辑视图
* 字段语义理解
* 对象建模页

---

### 2️⃣ Readonly State Token

**Component：`ReadonlyBadge`**

* 文案：`语义版本快照`
* Tooltip：`当前视图不可编辑`

---

## 十、设计给前端的 3 个关键说明（非常重要）

1. **这是同一套页面组件**

   * 通过 `mode = working | version`
2. **版本态只影响交互，不影响布局**
3. **所有编辑能力必须受 Version Context 控制**

---

## 十一、一句话给你设计/前端对齐

> **语义版本不是新页面，而是“治理快照视角”；
> 在 Figma 中体现为：同一组件在不同 Mode 下的交互差异。**

---

如果你愿意，下一步我可以直接帮你继续：

1️⃣ 把上面这些 **转成「Figma Component Tree（组件树）」**
2️⃣ 输出 **前端实现用的状态枚举 + Props 定义**
3️⃣ 帮你写 **「发布语义版本」的交互流程（含校验 / 拦截 / 异常）**

你选一个，我继续帮你把这一步彻底“工程化”。
